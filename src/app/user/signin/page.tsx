export default async function Signin() {

    // here we can use prisma
    // prisma.user.create({

    // })

    return (
        <div>
            <p>
                Sign in component
            </p>
        </div>
    )
}